# NOT USED by the self-host build any more: Dockerfile.selfhost is built from
# the REPO ROOT (it needs packages/engine), so docker reads the root
# .dockerignore instead. Kept for any build that still roots here; edit both.
.git
.github
.claude
.logs
**/.wrangler
**/node_modules
**/dist
**/coverage
*.log
Dockerfile
.env
.env.*
!.env.example
.alchemy/
